<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si on demande déjà /public, ne touche pas
    RewriteRule ^public/ - [L]

    # Tout le reste -> /public/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
